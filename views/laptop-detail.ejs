<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= laptop.name %> - LaptopStore</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header>
    <nav>
      <div class="container">
        <div class="logo">
          <h1>💻 LaptopStore</h1>
        </div>
        <ul class="nav-links">
          <li><a href="/">Trang chủ</a></li>
          <li><a href="/laptops" class="active">Sản phẩm</a></li>
          <li><a href="/about">Giới thiệu</a></li>
          <li><a href="/contact">Liên hệ</a></li>
          <% if (locals.user) { %>
            <li><a href="/profile">👤 <%= user.username %></a></li>
            <li><a href="/logout">Đăng xuất</a></li>
          <% } else { %>
            <li><a href="/login">Đăng nhập</a></li>
            <li><a href="/register">Đăng ký</a></li>
          <% } %>
        </ul>
      </div>
    </nav>
  </header>

  <main>
    <section class="product-detail">
      <div class="container">
        <div class="detail-grid">
          <div class="detail-image">
            <img src="<%= laptop.image %>" alt="<%= laptop.name %>">
          </div>
          <div class="detail-info">
            <h2><%= laptop.name %></h2>
            <p class="price large"><%= laptop.price.toLocaleString('vi-VN') %>đ</p>
            <p class="description"><%= laptop.description %></p>
            
            <div class="specs-detail">
              <h3>Thông số kỹ thuật</h3>
              <table>
                <tr>
                  <td><strong>CPU:</strong></td>
                  <td><%= laptop.specs.cpu %></td>
                </tr>
                <tr>
                  <td><strong>RAM:</strong></td>
                  <td><%= laptop.specs.ram %></td>
                </tr>
                <tr>
                  <td><strong>Ổ cứng:</strong></td>
                  <td><%= laptop.specs.storage %></td>
                </tr>
                <tr>
                  <td><strong>Màn hình:</strong></td>
                  <td><%= laptop.specs.screen %></td>
                </tr>
              </table>
            </div>

            <div class="actions">
              <button class="btn btn-primary">Mua ngay</button>
              <button class="btn btn-secondary">Thêm vào giỏ</button>
            </div>
            
            <a href="/laptops" class="back-link">← Quay lại danh sách</a>
          </div>
        </div>
      </div>
    </section>
  </main>

  <footer>
    <div class="container">
      <p>&copy; 2025 LaptopStore. All rights reserved.</p>
    </div>
  </footer>
</body>
</html>
