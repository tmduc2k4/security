<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Giỏ hàng - Security App</title>
  <link rel="stylesheet" href="/css/modern-style.css">
  <style>
    .breadcrumb {
      background: #f9fafb;
      padding: 1rem 0;
      margin-bottom: 2rem;
      border-bottom: 1px solid var(--border-gray);
    }

    .breadcrumb a {
      color: var(--primary-blue);
      text-decoration: none;
    }

    .breadcrumb a:hover {
      text-decoration: underline;
    }

    .cart-container {
      display: grid;
      grid-template-columns: 1fr 350px;
      gap: 2rem;
      margin-bottom: 3rem;
    }

    .cart-items {
      background: white;
      border-radius: 1rem;
      border: 1px solid var(--border-gray);
      overflow: hidden;
    }

    .cart-header {
      padding: 1.5rem;
      background: #f9fafb;
      border-bottom: 1px solid var(--border-gray);
      font-weight: 700;
      color: var(--text-dark);
    }

    .cart-item {
      display: grid;
      grid-template-columns: 100px 1fr 100px 80px;
      gap: 1.5rem;
      padding: 1.5rem;
      border-bottom: 1px solid var(--border-gray);
      align-items: center;
    }

    .cart-item:last-child {
      border-bottom: none;
    }

    .item-image {
      width: 100px;
      height: 100px;
      object-fit: cover;
      border-radius: 0.5rem;
      background: #f9fafb;
    }

    .item-info h3 {
      margin: 0 0 0.5rem 0;
      color: var(--text-dark);
      font-size: 0.95rem;
    }

    .item-info p {
      margin: 0.25rem 0;
      color: var(--text-gray);
      font-size: 0.85rem;
    }

    .item-info .price {
      color: var(--primary-blue);
      font-weight: 700;
      margin-top: 0.5rem;
    }

    .qty-control {
      display: flex;
      align-items: center;
      border: 1px solid var(--border-gray);
      border-radius: 0.5rem;
      overflow: hidden;
    }

    .qty-btn {
      background: #f9fafb;
      border: none;
      padding: 0.4rem 0.6rem;
      cursor: pointer;
      color: var(--primary-blue);
      font-weight: 600;
    }

    .qty-btn:hover {
      background: var(--primary-blue);
      color: white;
    }

    .qty-display {
      width: 40px;
      text-align: center;
      border: none;
      font-weight: 600;
      padding: 0.4rem 0;
    }

    .remove-btn {
      background: #fee2e2;
      color: #7f1d1d;
      border: none;
      padding: 0.5rem 1rem;
      border-radius: 0.5rem;
      cursor: pointer;
      font-weight: 600;
      font-size: 0.85rem;
      transition: all var(--transition-fast);
    }

    .remove-btn:hover {
      background: #fecaca;
    }

    .empty-cart {
      text-align: center;
      padding: 3rem;
      color: var(--text-gray);
    }

    .empty-cart-icon {
      font-size: 4rem;
      margin-bottom: 1rem;
    }

    .empty-cart a {
      display: inline-block;
      margin-top: 1.5rem;
    }

    .cart-summary {
      background: white;
      border-radius: 1rem;
      border: 1px solid var(--border-gray);
      padding: 1.5rem;
      height: fit-content;
      position: sticky;
      top: 100px;
    }

    .summary-title {
      font-size: 1.1rem;
      font-weight: 700;
      margin-bottom: 1.5rem;
      color: var(--text-dark);
      padding-bottom: 1rem;
      border-bottom: 2px solid var(--primary-blue);
    }

    .summary-row {
      display: flex;
      justify-content: space-between;
      margin-bottom: 1rem;
      color: var(--text-gray);
    }

    .summary-row.total {
      border-top: 1px solid var(--border-gray);
      padding-top: 1rem;
      color: var(--text-dark);
      font-weight: 700;
      font-size: 1.1rem;
    }

    .summary-row.total .label {
      color: var(--primary-blue);
    }

    .checkout-btn {
      width: 100%;
      padding: 0.75rem;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      border: none;
      border-radius: 0.5rem;
      font-weight: 700;
      cursor: pointer;
      margin-top: 1.5rem;
      transition: all var(--transition-fast);
    }

    .checkout-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 20px rgba(102, 126, 234, 0.3);
    }

    .continue-shopping {
      width: 100%;
      padding: 0.75rem;
      background: #f9fafb;
      color: var(--primary-blue);
      border: 2px solid var(--primary-blue);
      border-radius: 0.5rem;
      font-weight: 600;
      cursor: pointer;
      margin-top: 0.75rem;
      transition: all var(--transition-fast);
    }

    .continue-shopping:hover {
      background: #eff6ff;
    }

    .promo-code {
      margin-top: 1.5rem;
      padding-top: 1.5rem;
      border-top: 1px solid var(--border-gray);
    }

    .promo-label {
      font-size: 0.85rem;
      color: var(--text-gray);
      margin-bottom: 0.5rem;
      display: block;
    }

    .promo-input {
      width: 100%;
      padding: 0.5rem;
      border: 1px solid var(--border-gray);
      border-radius: 0.5rem;
      margin-bottom: 0.5rem;
    }

    .promo-btn {
      width: 100%;
      padding: 0.5rem;
      background: #f9fafb;
      border: 1px solid var(--border-gray);
      border-radius: 0.5rem;
      cursor: pointer;
      font-weight: 600;
      font-size: 0.85rem;
      color: var(--text-dark);
    }

    @media (max-width: 768px) {
      .cart-container {
        grid-template-columns: 1fr;
      }

      .cart-item {
        grid-template-columns: 80px 1fr;
        gap: 1rem;
      }

      .item-image {
        width: 80px;
        height: 80px;
      }

      .cart-summary {
        position: static;
      }
    }
  </style>
</head>
<body>
  <header>
    <nav>
      <div class="container">
        <div class="logo">
          <h1>🔐 Security App</h1>
        </div>
        <ul class="nav-links">
          <li><a href="/">Trang chủ</a></li>
          <li><a href="/laptops">Sản phẩm</a></li>
          <li><a href="/about">Giới thiệu</a></li>
          <li><a href="/contact">Liên hệ</a></li>
          <% if (locals.currentUser) { %>
            <li><a href="/profile">👤 <%= currentUser.username %></a></li>
            <li><a href="/logout">Đăng xuất</a></li>
          <% } else { %>
            <li><a href="/login">Đăng nhập</a></li>
          <% } %>
        </ul>
      </div>
    </nav>
  </header>

  <main>
    <div class="container">
      <!-- Breadcrumb -->
      <div class="breadcrumb">
        <a href="/">Trang chủ</a> / <strong>🛒 Giỏ hàng</strong>
      </div>

      <h1 style="margin-bottom: 2rem; color: var(--text-dark);">🛒 Giỏ hàng của bạn</h1>

      <% if (typeof items !== 'undefined' && items.length > 0) { %>
        <div class="cart-container">
          <!-- Cart Items -->
          <div class="cart-items">
            <div class="cart-header">
              <div style="display: grid; grid-template-columns: 100px 1fr 100px 80px; gap: 1.5rem;">
                <div>Hình ảnh</div>
                <div>Sản phẩm</div>
                <div>Số lượng</div>
                <div>Hành động</div>
              </div>
            </div>

            <% items.forEach(item => { %>
              <div class="cart-item">
                <img src="<%= item.image %>" alt="<%= item.name %>" class="item-image">
                <div class="item-info">
                  <h3><%= item.name %></h3>
                  <p><%= item.specs.cpu %></p>
                  <p class="price"><%= item.price.toLocaleString('vi-VN') %>đ</p>
                </div>
                <div class="qty-control">
                  <button class="qty-btn">−</button>
                  <input type="number" class="qty-display" value="<%= item.quantity || 1 %>">
                  <button class="qty-btn">+</button>
                </div>
                <button class="remove-btn">Xóa ✕</button>
              </div>
            <% }); %>
          </div>

          <!-- Cart Summary -->
          <div class="cart-summary">
            <div class="summary-title">📊 Tóm tắt đơn hàng</div>
            
            <div class="summary-row">
              <span>Tạm tính:</span>
              <span id="subtotal">0đ</span>
            </div>
            <div class="summary-row">
              <span>Phí vận chuyển:</span>
              <span class="text-green">Miễn phí 🎁</span>
            </div>
            <div class="summary-row">
              <span>Thuế:</span>
              <span id="tax">0đ</span>
            </div>
            <div class="summary-row total">
              <span class="label">Tổng cộng:</span>
              <span id="total">0đ</span>
            </div>

            <button class="checkout-btn">Thanh toán</button>
            <button class="continue-shopping" onclick="location.href='/laptops'">← Tiếp tục mua sắm</button>

            <div class="promo-code">
              <label class="promo-label">Mã khuyến mãi (nếu có):</label>
              <input type="text" class="promo-input" placeholder="Nhập mã khuyến mãi">
              <button class="promo-btn">✓ Áp dụng</button>
            </div>
          </div>
        </div>
      <% } else { %>
        <div class="empty-cart">
          <div class="empty-cart-icon">🛒</div>
          <h2>Giỏ hàng trống</h2>
          <p>Bạn chưa có sản phẩm nào trong giỏ hàng</p>
          <a href="/laptops" class="btn btn-primary">Tiếp tục mua sắm</a>
        </div>
      <% } %>
    </div>
  </main>

  <footer>
    <div class="container">
      <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 2rem; margin-bottom: 2rem;">
        <div>
          <h4>🔐 Security App</h4>
          <p>Laptop chất lượng cao với giá cạnh tranh</p>
        </div>
        <div>
          <h4>📚 Hỗ trợ</h4>
          <ul style="list-style: none;">
            <li><a href="/about">Giới thiệu</a></li>
            <li><a href="/contact">Liên hệ</a></li>
            <li><a href="/faq">Câu hỏi thường gặp</a></li>
          </ul>
        </div>
        <div>
          <h4>🛒 Mua sắm</h4>
          <ul style="list-style: none;">
            <li><a href="/laptops">Sản phẩm</a></li>
            <li><a href="/orders">Đơn hàng</a></li>
            <li><a href="/track">Theo dõi</a></li>
          </ul>
        </div>
      </div>
      <div class="footer-bottom">
        <p>&copy; 2025 Security App. Bán laptop an toàn, uy tín.</p>
      </div>
    </div>
  </footer>

  <script>
    // Calculate totals (demo)
    function calculateTotals() {
      // TODO: Implement real calculation
      document.getElementById('subtotal').textContent = '0đ';
      document.getElementById('tax').textContent = '0đ';
      document.getElementById('total').textContent = '0đ';
    }

    calculateTotals();
  </script>
</body>
</html>
